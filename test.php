<?php

// post > crud > read(all, one) . insert(image, data) . update . delete(unlink)

// users > register . login > authentication (admin)

// authorization > permissions

// pagination > limit . offset

// localization ( a, e )